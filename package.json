{
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.0.0",
    "react-router-dom": "^7.13.0",
    "vite": "^7.3.1"
    },
  "scripts": {
    "vercel:install": "npm install && npm install --prefix frontend && npm install --prefix backend && npm install --prefix backend/functions",
    "vercel:build": "npm run build --prefix frontend && rm -rf dist && cp -r frontend/dist dist"
  }
}
